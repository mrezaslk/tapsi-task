# تکنیکال تسک 

هدف اصلی این پروژه  یک اپلیکیشن React با SSR سفارشی روی Node.js پیاده‌سازی شود که:

- برنامه هم قابلیت سرور ساید رندرینگ (SSR) رو داره، یعنی صفحات اول از سمت سرور ساخته می‌شن تا سریع‌تر نمایش داده بشن.
- با این حال تجربه‌ی کاربری مثل یه **اپلیکیشن تک‌صفحه‌ای (SPA)** خواهد بود و با کمک React Router می‌تونید توی کلاینت جابجا بشید.
- یک **API ساده** هم برای دریافت داده‌ها داریم که فرایند توسعه  راحت‌تر می‌کنه.
- از **ETag** استفاده کردیم تا سرعت بارگذاری بهتر شه و داده‌ها به شکل هوشمند کش بشن.
- و ظاهر اپ کاملاً **موبایل فرست** و با TailwindCSS طراحی شده، پس روی گوشی هم عالی کار می‌کنه!

---


## 🚀 نحوه‌ی اجرا در محیط توسعه

### 1. نصب وابستگی‌ها

```bash
yarn install
```

یا

```bash
npm install
```

---

### 2. اجرای سرور Node (SSR + API) روی پورت 3000

```bash
yarn dev:server
```

یا

```bash
npm run dev:server
```

---

### 3. اجرای Vite dev server برای کلاینت روی پورت 5173

```bash
yarn dev:client
```

یا

```bash
npm run dev:client
```

---

## 🏗️ نحوه‌ی ساخت (Build) پروژه 

برای ساخت پروژه و آماده‌سازی برای محیط production، مراحل زیر را دنبال کنید:

### 1. ساخت فایل‌های کلاینت با Vite

در این مرحله، فایل‌های React کلاینت با Vite کامپایل می‌شوند و در پوشه `dist/client` ذخیره می‌شوند:

```bash
yarn vite build
```

یا

```bash
npm run vite build
```



---

### 2. کامپایل سرور با TypeScript

برای کامپایل کردن کدهای TypeScript سرور به JavaScript:

```bash
yarn tsc
```

یا

```bash
npx tsc
```



---

### 3. اجرای پروژه در حالت Production

پس از انجام مراحل ساخت، برای اجرای پروژه در حالت production:

```bash
node dist/server/index.js
```

سرور روی پورت 3000 اجرا می‌شود و هم API و هم صفحات SSR را سرو می‌دهد.


